{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn workspace bad-redux-demo run start",
    "build": "yarn workspace bad-redux run build",
    "test": "yarn workspace bad-redux run test"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
