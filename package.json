{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test -- --watch=false"
  },
  "devDependencies": {
    "turbo": "^1.5.6"
  },
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ]
  },
  "packageManager": "yarn@3.4.1"
}
