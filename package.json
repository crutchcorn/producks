{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "yarn workspace bgs-demo run start",
    "build": "yarn workspace bgs run build",
    "test": "yarn workspace bgs run test",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
